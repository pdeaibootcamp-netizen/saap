.claude/settings.local.json
.DS_Store

# Node / Next.js
src/node_modules/
src/.next/
src/next-env.d.ts
src/.env.local
src/.env*.local
src/tsconfig.tsbuildinfo

# Industry-data hand-offs — held local-only by default until the user OKs
# committing them to a public repo (repo is currently public per gh repo view).
PRD/industry-data/*.xlsx
PRD/industry-data/*.xls
PRD/industry-data/*.csv

# Local data folder (raw imports, exports, scratch files — never commit)
/data/
