# Decision Log

*Owner: orchestrator. Cross-cutting decisions only — single-domain decisions live in the owning specialist's artifact (e.g., ADRs in `docs/engineering/`, PDRs in `docs/product/`).*

| Date | ID | Decision | Rationale | Rejected alternatives | Author |
|---|---|---|---|---|---|
| 2026-04-17 | D-001 | MVP will use hand-assigned cohorts on pre-populated data. | Closes the MVP scope gap called out in PRD §9: percentile calculation needs cohort segmentation matching and owner data ingestion, but both are scheduled for Increment 3. Hand-assignment on pre-populated data keeps embedded benchmark value in MVP without pulling full onboarding + data-ingestion forward. | (a) Pull cohort-assignment and data-ingestion features into MVP — inflates scope and delays the first brief; (c) Ship MVP briefs with sector-level data only and defer per-user embedded benchmarks to Increment 2 — weakens day-one proof of value. | orchestrator |
| 2026-04-17 | D-002 | No RM Lead Signal Surface at MVP. | PRD §8.3 and §9 list this as an open question. The RM program, consent UX, and opportunity-framing rules aren't ready; shipping a lead surface prematurely risks triggering the #1 trust barrier (fear that data feeds ČS credit risk — PRD §3, §13.3). Goal #3 (RM lead generation) is deferred one increment rather than compromised. | Manual analyst-curated weekly list; dedicated RM-facing view in George; export feed into existing RM tooling — all require consent UX and signal-framing work we haven't completed. | orchestrator |
| 2026-04-17 | D-003 | MVP metric list = 8 ratios: gross margin, EBITDA margin, labor cost ratio, working capital cycle, ROCE, revenue growth vs cohort median, pricing power proxy, revenue per employee. | Phase 0 Q0.1 + Q0.2. Balanced 7 (PM-recommended) spans both §5 categories and supports 2–4 observations per brief (§8.1); user added revenue per employee as an owner-legible productivity proxy, overriding the §4 operational-metrics non-goal on the grounds that the ratio is distinct from general operational KPIs. PM to freeze in `docs/product/mvp-metric-list.md` during Phase 1. | Profitability-focused 6 (too thin for verdict range); Strategic-forward 8 with capital intensity trend instead of rev/employee (heavier authoring without productivity legibility); excluding rev/employee (PRD-conservative default). | user |
| 2026-04-17 | D-004 | MVP language scope = Czech only. | Phase 0 Q0.3. Matches §3 persona (Czech SME owner-operator), §11 RM distribution (Czech), §9 roadmap (English scheduled Increment 5). Dual-language authoring would roughly double analyst effort (§13.1 risk) against an unvalidated audience. | Czech + English at MVP. | user |
| 2026-04-17 | D-005 | Brief cadence commitment to users deferred to backlog ([B-001](backlog.md)). | Phase 0 Q0.4. MVP is a time-boxed trial <1 month; no cadence commitment to end-users is needed for the trial window. Briefs remain human-authored and manually supplied (no automated generation or fetch, reaffirming CLAUDE.md guardrail). Trigger to reopen logged in B-001. | Monthly commitment; pre-loaded curated set; ad-hoc publishing with no cadence promise. | user |
| 2026-04-17 | D-006 | Brief personalization grain at MVP = sector (NACE) only. | Phase 0 Q0.5. Data-engineer-recommended. Size/region surface only via embedded benchmark snippet, which degrades independently when below the statistical-validity floor (§10, §13.5, CLAUDE.md cold-start guardrail). Under D-001 (hand-assigned cohorts on pre-populated data), sector-only grain keeps analyst authoring tractable. | Sector × size band (2–3× authoring); full NACE × size × region (multiplies authoring and sub-floor cells). | user |
| 2026-04-17 | D-007 | Consent data-primitive model at MVP = single opt-in covering all lanes. | Phase 0 Q0.6a. Simplest MVP implementation; minimal consent-ledger complexity. Acknowledged trade-off: may require a re-consent event when the RM lane activates post-MVP (D-002 deferred it). Legal review required regardless. | Per-lane toggles with RM dark (DE-recommended; rejected as YAGNI for MVP); bank-sourced default-on + user-contributed default-off. | user |
| 2026-04-17 | D-008 | Consent UX surface at MVP = single-screen declaration before first brief view. | Phase 0 Q0.6b. Designer-recommended. Plain-language four-lane declaration (brief / cohort / RM-visible / credit-risk); each lane written as reassurance (what the bank does NOT do, then what it does); revocation in Settings > Soukromí. Copy requires legal review — flagged as Phase 1 dependency. Note: D-007 single opt-in means the four-lane structure is educational (transparency), not per-lane toggles; designer + data-engineer must align revocation semantics in Phase 1. | Inline badges at each data-touch (legal adequacy unclear); progressive per-increment prompts (risks trust-barrier event mid-relationship). | user |
| 2026-04-17 | D-009 | Accountant/advisor sharing at MVP deferred to backlog ([B-002](backlog.md)). | Phase 0 Q0.7. Same trial-duration rationale as D-005: MVP is a time-boxed trial; sharing not needed for trial duration. Note: PDF download via §9 Multi-Format Delivery still exists; owners can forward the file themselves without in-product sharing infrastructure. | PDF forwarding only (marked as default); read-only share link; named advisor seat (would be give-to-get plumbing, Increment 3 per CLAUDE.md guardrail). | user |
| 2026-04-17 | D-010 | Canonical data-lane identifiers = `brief` / `user_contributed` / `rm_visible` / `credit_risk`. | Phase 1 gate reconciliation. Data-engineer's draft privacy-architecture.md used `brief` / `user` / `rm-lead` / `credit-aware-boundary` in the consent ledger `lanes_covered` field; engineer's ADR-0002 used `brief` / `user_contributed` / `rm_visible` / `credit_risk` in the storage `data_lane` enum. Same conceptual lanes, drifted identifiers. Canonical form is snake_case, SQL-safe, and more descriptive than DE's short forms. Applies to: consent ledger `lanes_covered` values, storage `data_lane` enum, all prose references in `docs/data/` and `docs/engineering/`. Storage-namespace names (`brief_db`, `user_db`, `rm_lead_db`) stay as infrastructure identifiers — they are not lane identifiers. Resolves engineer Q-005. | Keep DE's shorter forms (`user`, `rm-lead`) — rejected as too terse and potentially ambiguous (`user` collides with user-object vocabulary); keep `credit-aware-boundary` — rejected as awkward and better explained in architecture prose than in an enum. | orchestrator |
| 2026-04-17 | D-011 | Canonical benchmark categories and ratio assignments per [mvp-metric-list.md](../product/mvp-metric-list.md) §Category grouping. The four categories (user-facing Czech) are: **Ziskovost** (Gross margin, EBITDA margin); **Náklady a produktivita** (Labor cost ratio, Revenue per employee); **Efektivita kapitálu** (Working capital cycle, ROCE); **Růst a tržní pozice** (Revenue growth vs cohort median, Pricing power proxy). | Phase 1 gate reconciliation. PM's `mvp-metric-list.md` and designer's `information-architecture.md` drifted on category names ("Náklady a produktivita" vs "Nákladová struktura"; "Růst a tržní pozice" vs "Tržní pozice") and on ratio assignments (Cat 2/3 composition differs). PM owns `docs/product/` per CLAUDE.md ownership; category names + groupings are product-level decisions. Designer IA will be updated to match. Resolves designer Q-001. | Designer's provisional naming + grouping — rejected: violates ownership table (design consumes product naming, not the other way around). | orchestrator |
| 2026-04-17 | D-012 | Revocation semantics under D-007 single opt-in = **Option A — stop future flow only.** No deletion of user-contributed rows; existing data and published aggregates are retained. The single revoke action fires a `revoke` consent event (see `privacy-architecture.md` §4); downstream pipelines check `consent_event_id` at read time and stop delivering briefs, stop ingesting new user-contributed data, and stop emitting the user into future cohort computations. | Phase 1 [OQ-001](open-questions.md). User-decided. Simplest implementation; no backfill/rebuild cost. Trade-off accepted: may not satisfy a GDPR Art. 17 "right to erasure" request from a user who explicitly invokes it — a separate erasure pathway may need to exist as a distinct Settings action (not "withdraw consent") when this becomes load-bearing. Flagged to legal review ([OQ-004](open-questions.md)). Resolves [OQ-001](open-questions.md); also resolves [OQ-009](open-questions.md) (hard-delete vs anonymize is moot since Option A retains rows). | Option B (stop + delete per-user, retain aggregates) — DE-recommended; rejected as over-built for <1-month trial. Option C (delete everything including aggregates) — rejected as operationally expensive and would shift already-delivered briefs' percentiles. | user |
| 2026-04-17 | D-013 | MVP managed Postgres = **Supabase Postgres.** Application hosting = **Vercel** (Next.js, unchanged from ADR-0001-F). | Phase 1 [OQ-002](open-questions.md). User-decided. Supabase gives managed Postgres with RLS (required for D-010 lane enforcement), plus auxiliary services (auth, storage, realtime) if later useful. Vercel stays as the Next.js hosting target for zero-ops preview deploys and serverless functions. Engineer updates `adr-0001-tech-stack.md` §F and `adr-0002-brief-storage-and-delivery.md` §A to reference Supabase instead of Vercel Postgres (Neon); RLS and lane-enforcement approach (ADR-0002-C) transfers unchanged — Supabase Postgres is upstream Postgres with RLS enabled. Resolves [OQ-002](open-questions.md). | Vercel + Vercel Postgres (Neon) — engineer-recommended; rejected by user. Azure + Azure DB (for strict ČS-policy alignment) — rejected for MVP; if ČS security review later requires Azure, revisit via new decision. | user |
| 2026-04-17 | D-014 | Phase 1 foundation gate **ratified**. All eight foundation artifacts (DE: `privacy-architecture.md`, `cohort-math.md`; EN: `adr-0001-tech-stack.md`, `adr-0002-brief-storage-and-delivery.md`; PM: `mvp-metric-list.md`, `assumption-log.md`; PD: `information-architecture.md`, `trust-and-consent-patterns.md`) landed. Cross-cutting naming drift reconciled (D-010, D-011). Load-bearing substantive decisions resolved (D-012 revocation semantics, D-013 Postgres provider). Reconciliation edits applied to all affected artifacts by their owning specialists. Phase 2 (feature workstreams per [build-plan.md](build-plan.md) §5) is unblocked. | Phase 1 gate per [build-plan.md](build-plan.md) §4. Remaining open items ([OQ-003](open-questions.md), OQ-004, OQ-005, OQ-006, OQ-007, OQ-008, OQ-013, OQ-021) are legal-review or ČS-liaison dependencies that do not block Phase 2 feature PRD drafting; they block production-readiness, not design/development. OQ-010–OQ-016 are specialist follow-ups owned inside Phase 2. OQ-017–OQ-020 are Increment 2+/post-trial deferrals. | N/A — gate ratification, not a choice between alternatives. | orchestrator |
| 2026-04-20 | D-015 | Phase 2 PM + PD gate **ratified** (trial-phases-2-4 branch). All 9 MVP feature PRDs and 9 design artifacts landed across Tracks A (monthly-briefing-generation, observation-generation, plain-language-translation, action-specificity-framing), B (percentile-position-calculation, quartile-position-display, category-based-layout), and C (sector-profile-configuration, multi-format-delivery). Canonical vocabulary extended: 6 glossary terms added (Brief artifact, Observation (Strategy Radar), Observation framing, Observation anchor, Time-horizon tag, Delivery record); frozen Czech quartile labels (horní čtvrtina / třetí čtvrtina / druhá čtvrtina / spodní čtvrtina); frozen time-horizon enum (Okamžitě / Do 3 měsíců / Do 12 měsíců / Více než rok); frozen per-category empty-state copy template. Phase 3 implementation (engineer + data-engineer sector-profile addendum) is unblocked. | Phase 2 PM+PD gate per [build-plan.md](build-plan.md) §5. 67 specialist OQs indexed in [open-questions.md](open-questions.md) by feature artifact; 9 promoted as cross-cutting OQ-045..053. None block Phase 3 scaffold or feature implementation; they resolve during engineering or stay non-blocking. | N/A — gate ratification. | orchestrator |
| 2026-04-20 | D-016 | Phase 3 implementation **partial-ratified** (trial-phases-2-4 branch). Scaffold (Next.js 14 App Router + TypeScript + Supabase Postgres + RLS migrations 0001–0005 + 10 migration-enum tests) and feature implementation (admin authoring at `src/app/admin/`, onboarding at `src/app/onboarding/`, consent gate at `src/app/consent/`, owner brief view at `src/app/brief/[id]`, revocation surface at `src/app/settings/soukromi/`, publish pipeline + stubbed email + stubbed PDF + cohort stub, seed script) landed. `tsx` added as devDependency for `npm run seed` — approved as dev-only tooling. One load-bearing bug found post-hoc (EN-001: `/api/consent/route.ts` called `grantConsent`/`revokeConsent` with wrong parameter names — `ip_address`/`user_agent` instead of `surface`/`channel`/`ip_prefix`) and fixed surgically before this ratification. | Phase 3 per [build-plan.md](build-plan.md) §6. "Partial" because subagent Bash access was restricted, so `npm install`, `npm run build`, `tsc --noEmit`, and `vitest run` were not executed during implementation — user must run locally before treating as green. 4 doc-drift / runtime items remain open (EN-002 source-enum drift, EN-003 table-naming drift, EN-004 cookie-consistency, missing `/fake-george` mock). Tracked in [phase-4-verification.md](phase-4-verification.md) known-gaps table. | N/A — gate ratification. | orchestrator |
| 2026-04-20 | D-017 | Phase 4 verification **initial paper pass complete** (trial-phases-2-4 branch). Eight check categories (V1–V8) covered in [phase-4-verification.md](phase-4-verification.md): 2–4-action bound enforcement, statistical-validity floor handling, no-raw-number-without-comparison, privacy-lane separation, three-format delivery fidelity, George embedding stub, time-to-first-verdict, revocation stop-flow. Each check marked STATIC PASS or RUNTIME REQUIRED with file+line evidence. Runtime checks are user-gated per the trial-branch contract (user installs, migrates, seeds, and clicks through locally). | Phase 4 per [build-plan.md](build-plan.md) §7. This is a trial-branch pass; the production path will run Phase 4 with CI-driven execution of the runtime checks. Any runtime failure the user surfaces returns the relevant feature to its owning specialist, not a silent src/ patch. | N/A — gate ratification. | orchestrator |
| 2026-04-21 | D-018 | v0.2 dashboard header brand mark = **"Česká Spořitelna · Strategy Radar"**, matching the brief page. | Resolves [OQ-056](open-questions.md). User-decided. Keeps bank-native framing consistent across owner-facing surfaces, reinforcing the trust-transfer mechanism in PRD §12 and the "bank-native distribution" guardrail in [CLAUDE.md](../../CLAUDE.md). | "Strategy Radar only" — rejected as inconsistent with the brief page and weaker on day-one trust signal. | user |
| 2026-04-21 | D-019 | v0.2 dashboard list heading for briefs = **"Analýzy"**. Glossary gains a new user-facing term; "Analýzy" (plural) = the dashboard list of sector-briefs, "Přehled" (singular) = one brief document. | Resolves [OQ-055](open-questions.md). User-decided. Cleanly separates the list label from the content-unit label, closing the collision called out by PM in `dashboard-v0-2.md` §12. PM must extend `docs/product/glossary.md` with the new entry when taking the next copy pass. | "Přehledy" (PM preference — preserved the collision); "Aktuální přehledy pro váš obor" (descriptive, still overloaded the word "přehled"); "Souhrn"/"Výhled" as page-name (renamed the page rather than the list — rejected as more invasive than needed). | user |
| 2026-04-21 | D-020 | v0.2 brief-page publication placement = **hybrid (layperson opener visible + full ČS publication collapsed)** with the in-brief label **"Sektorová analýza"**. Layperson opener is a 200–400-word plain-Czech owner-voice distillation of the ČS publication; full text is behind a "Číst celou analýzu" disclosure. Source attribution cites Ekonomické a strategické analýzy České spořitelny. Brief page's former "Srovnávací přehled" benchmark section is removed at v0.2 (benchmarks now live on the dashboard tiles; the BenchmarkSnippet stays on the brief row for analyst-side reference and possible v0.3 reintroduction). Insight↔action connection = each `ClosingAction` carries an optional `paired_observation_index`; paired actions render adjacent to their observation, orphans go under a separate "Další doporučené kroky" section that omits when empty — additive to v0.1 `BriefContent`, no v0.1 brief breaks. | PM artifact `docs/product/brief-page-v0-2.md`. Hybrid satisfies both verdict-first / day-one proof-of-value (opener is scannable in ~45s) and the trust-transfer function the ČS source provides (full text is inspectable). Full-text-default rejected: violates plain-language + verdict-first principles; owner reads 21k Czech characters only on rare deep-dive. Simplified-only rejected: removes the bank-native source the owner may want to verify. Glossary term "Sektorová analýza" chosen over "Analýza" singular to avoid collision with D-019 "Analýzy" plural (dashboard list heading). Pairing model is additive — no migration, no v0.1 break. | Full-text-default; simplified-only (no disclosure); singular "Analýza" as in-brief label (rejected as semantically colliding with D-019). | product-manager |
| 2026-04-27 | D-021 | **Trial branches `trial-v0-2` and `trial-v0-2-GDS` frozen via GitHub branch ruleset 15590034** after the v0.2 → main merge (`0888ab7`, no-ff). Ruleset rules: `update` (no tip moves), `non_fast_forward` (no force-push), `deletion`. `bypass_actors: []` and `current_user_can_bypass: never` — even repo admins cannot push to or delete these branches without first removing the ruleset. `trial-phases-2-4` (v0.1 reference) intentionally left unprotected at this gate; protection can be extended later if it becomes load-bearing. View / edit at https://github.com/pdeaibootcamp-netizen/saap/rules/15590034. | User-decided ("freeze both of the trial branches"). Both branches are fully contained in `main` (`git log origin/main..origin/trial-v0-2` and `…trial-v0-2-GDS` both empty), so any further commits on them would diverge silently. Server-side ruleset is stronger than convention because it survives accidental local pushes. | Branch protection via classic API (older, less flexible than rulesets); local discipline only (no enforcement, fragile to accidental push); rename to `archive/*` (cosmetic, doesn't prevent writes). | user |
| 2026-04-27 | D-022 | v0.3 working branch = **`trial-v0-3`** (created off `main` `0888ab7`). All v0.3 work — `owner_metrics` schema migration, percentile compute, in-tile prompt UX, n8n analysis pipeline — lands on this branch. `main` is not touched directly. Merge back to `main` on v0.3 completion follows the same `--no-ff` pattern used for v0.2. | Established pattern: feature work happens on a `trial-v0-N` branch, customer-test reference state stays on `main`, completed work merges with a merge commit. Direct work on `main` rejected because it would collide with any in-flight v0.2 customer testing using the `main` build. | Direct commits to `main`; per-track sub-branches (`trial-v0-3-trackA`, etc. — rejected as ceremonial when one branch suffices). | orchestrator |
| 2026-04-27 | D-023 | **v0.3 demo owner = a selectable real Czech firm by IČO**, not a hardcoded synthetic profile. The dashboard surfaces a small IČO input field in the header band (dev-only / PoC, persists in cookie). Entering an IČO that matches a row in `cohort_companies` switches the active demo owner to that firm; the firm's actual financials populate the tiles where the data exists; gaps in the firm's row drive the "ask when missing" CTAs (D-018 / Track A). A small set of pre-seeded demo IČOs is loaded at build time so the moderator has known good firms to pick from when starting cold. | User-decided. Synthetic null/value mix was rejected on the basis that the demo is much more compelling — and tests the give-to-get product mechanic more honestly — when the gaps the owner is asked to fill are real gaps in real data. Replaces the v0.3 plan's original "clear 2 of 8 metrics" approach. The pre-seeded firms remain a fallback so the dashboard never opens to a blank state. | URL-only `?firm=ICO` query param (rejected — moderator-unfriendly during live testing); picker dropdown with all 4500+ firms (rejected — overkill, hard to choose well during a moderated session); single hardcoded firm per env (rejected — moderator can't switch live). | user |
| 2026-04-27 | D-024 | **v0.3 frozen-metric set: replace ROCE with Net margin (Čistá marže = Hospodářský výsledek / Obrat × 100).** Updates D-015's frozen 8-metric list. Net margin lives in the Ziskovost (profitability) category alongside gross margin and EBITDA margin; ROCE is dropped from Efektivita kapitálu, leaving Working capital cycle as the sole metric in that category. The 4 D-011 categories stay, but the per-category metric counts move from 2-2-2-2 to 3-2-1-2 (Ziskovost-Náklady-Efektivita-Růst). | The current industry-data Excel for NACE 49.41 contains revenue, profit, employee count, and city — nothing that supports ROCE (no equity / capital-employed line). Net margin, by contrast, is computable for 61% of firms in the dataset directly. ROCE was the lowest-coverage metric on the frozen list given typical Czech registry data; net margin is the highest-coverage one not yet in the set. The asymmetric category count is accepted as a known tradeoff for v0.3; if richer data arrives that covers ROCE directly, this decision may be revised before Track A implementation lands and the original 2-2-2-2 grid restored. | Keep ROCE and accept all 8 tiles cold (Excel supports only 1 metric directly, leaves 7 in "ask" state — too heavy a CTA load); reshape the frozen-8 by NACE per Excel coverage (rejected — design-by-data contradicts the frozen-metric product principle); add net margin as a 9th tile without dropping ROCE (rejected — breaks the 2-2-2-2 grid in the opposite direction and tile composition was already fixed at 8). | user |
| 2026-04-27 | D-025 | **v0.3 cohort percentile computation uses a synthetic per-NACE quintile fallback for any (NACE, metric) cell not covered by ingested real industry data.** Synth values are generated per metric per NACE to be sector-plausible (informed by published Czech-sector financial medians and typical dispersion patterns); stored in `cohort_aggregates` with `source = 'synthetic'`; clearly distinguishable from real-data percentiles in the DB. The runtime percentile compute treats both sources uniformly — tiles render identically regardless of source — but the source is logged for audit and surfaced in any debug / analyst view. Real-data cells supersede synth when both exist for the same NACE × metric. Production deployment with real customers must replace synth cells before launch; synth fallback is a PoC posture only. | Without this fallback, 6 of 8 frozen metrics would be permanently in the "ask" or below-floor state for narrow-data NACEs like 49.41, leaving the cold-load dashboard with one populated tile and seven CTAs — too heavy a give-to-get demand for a customer-testing demo. User-decided: "assume richer data arrives; for whatever we don't get, generate believable quintile values per NACE; we'll change this later if needed." Synth is explicitly tagged so it's never confused with real data and is trivially swappable when richer datasets land. | Pure fixture path (rejected — the v0.2 fixtures are obviously synthetic and break the "real data" pivot the user is now making); real data only with degradation rungs everywhere else (rejected — produces an unusable cold-load demo, leaves 7/8 tiles in CTA state); hold v0.3 until richer data is in (rejected — user explicitly opted to proceed with synth fillers and adjust later); 9th synth-only tile labelled as such (rejected — breaks the frozen 8 grid and surfaces an internal data-source detail to the owner). | user |
| 2026-04-28 | D-026 | **Track C analysis automation runs on Claude Code (Max subscription) as the orchestrator, with n8n shrunk to a single-tool MCP persistence layer.** The 18-node `analysis-automation.json` API-key workflow stays in repo as the frozen alternative but is not used. The new MCP-based architecture: analyst opens Claude Code in terminal, attaches a publication PDF/DOCX, Claude Code parses + summarises + spawns parallel NACE sub-agents via the Task tool, each sub-agent calls a single `write_draft_brief` MCP tool exposed by n8n. n8n's role collapses to: receive structured fields → HMAC-sign → POST `/api/admin/briefs/from-n8n`. | Per-run cost drops from ~$0.50 (10 LLM calls × Sonnet API) to $0 because Claude Code uses the user's existing Max subscription quota. Trade-off accepted: analyst UX moves from browser admin page (`/admin/publications/new`) to a terminal-driven Claude Code session — fine for the moderator-driven PoC; would need to be revisited if real ČS analysts are onboarded directly. Privacy posture preserved (the publication body never persists in n8n; only the structured callback transits). The original Track C workflow (`analysis-automation.json`) stays in repo as the API-key alternative in case the subscription path becomes unworkable. | n8n + Anthropic API (~$0.50/run, browser UX preserved) — rejected: cost; n8n + cheaper LLM provider (DeepSeek / Gemini Flash, ~$0.05/run) — rejected: Czech long-form quality drop; abandon Track C — rejected: analyst hand-authoring is the v0.1 baseline we already proved is too slow. | user |
| 2026-04-28 | D-027 | **v0.3 Track C PoC scoped to NACE 31 (furniture) + NACE 49 (road freight) only.** The two-NACE pipeline is the structural template; food (NACE 10 or 11) and aluminium-wholesale (NACE 46.74) are deferred until industry-data Excels for those NACEs arrive. The orchestration skill spawns N parallel sub-agents (N=2 today, configurable to 4 when the additional Excels land); the existing ingest script + bucket parser handle the new NACEs without code changes. | Cohort-percentile compute requires `cohort_companies` rows for each NACE division. NACE 31 + 49 have ingested data (3,579 + ~380 firms). NACE 10/11 + 46 have none, so percentile tiles for those would be entirely synth-quintile fallback — possible but not validated against real distributions. User chose to wait for the real Excels rather than ship a 4-NACE demo half on real data and half on synth. The pipeline is structurally NACE-parallel from the start so adding NACE 10/11 + 46 later is a configuration change, not a redesign. | Build all 4 NACEs now with synth-only fallback for the 2 missing — rejected: dilutes the demo's claim of "real data" and risks divergence between the documented and actual cohort distributions; drop the Track C PoC to single-NACE — rejected: misses the parallel-orchestration design intent the user wants validated; stub the missing 2 NACEs with hand-typed fake firms — rejected: same dilution problem as synth-only with extra maintenance. | user |
| 2026-04-28 | D-028 | **Track C runs on n8n + Anthropic API in n8n (Claude Haiku 4.5). Supersedes D-026.** The MCP-orchestrator path was tried for ~half a session; n8n's tool nodes (`toolCode`, `toolWorkflow`) require an AI Agent upstream to populate `$fromAI` / `$input`, which doesn't exist in raw-MCP context — tool args arrived empty regardless of how the JSON Schema was authored. Switching to API-mode in n8n means the langchain.agent nodes call Anthropic directly with proper input. Browser upload UX is preserved. Per-run cost ~$0.04–0.10 with Haiku (5–9 calls per publication). The 18-node `analysis-automation.json` (D-026's API-key fallback) and `mcp-draft-writer.json` (the abandoned MCP attempt) stay in `n8n-workflows/` as historical reference. | The Anthropic Max subscription that backed D-026 doesn't cover generic API use; the user opted to fund a small API allotment to unblock the live design. Cost remains within v0.3 PoC budget; quality from Haiku has been adequate on the publications tested. n8n's MCP-tool-routing model is what blocked the original plan, not Claude Code itself. | Pay for Sonnet via the API (~4× cost, no measured PoC quality benefit on these publications); self-host a Claude Code orchestrator pinging n8n via webhook (effort + brittle around tool-call propagation through sub-agents); accept the MCP routing limitation and write a thin n8n workflow trigger as a workflow-tool wrapper (rejected — adds an inner workflow + we'd still need to handle the per-NACE fan-out manually). | user |
| 2026-04-28 | D-029 | **Brief data model = Model B (multi-NACE-tagged).** One brief row per publication; `briefs.nace_sectors text[]` carries the relevance set; per-NACE content lives inside `BriefContent.per_nace_content[naceDivision]` with the same `{observations, closing_actions}` shape used by v0.1/v0.2. Legacy `briefs.nace_sector` column = `nace_sectors[0]` for backward compat with code that still expects a singular column. Customer dashboard filter changes from `WHERE nace_sector = ?` to `WHERE active_firm_nace = ANY(nace_sectors)`. Migration 0011 adds the column + GIN index, backfills existing rows from `nace_sector`. | One review per publication beats N reviews — the analyst sees all relevant NACEs in one place and decides which to publish. Eliminates duplicate-write fan-out. Customers in any covered NACE see the brief on their dashboard with their NACE's content rendered (the brief page picks `per_nace_content[activeNace]`). | Model A (one brief row per (publication × NACE)) — rejected: 4× analyst review burden, dedupe complexity at update time; duplicate the brief per NACE at write time — rejected: same problems plus storage overhead; tag with the *primary* NACE only and let the analyst manually copy content into other briefs — rejected: defeats the multi-NACE generation entirely. | user |
| 2026-04-28 | D-030 | **Per-NACE relevance gate at the Insights agent.** Each NACE branch outputs `{relevant: bool, insights?: [...], reason?: string}`. Branches with `relevant: false` skip the Actions agent entirely and emit a tag-with-reason for the Compose Bundle. Compose Bundle filters to relevant NACEs only when building `nace_sectors` and `per_nace_content`. The `diagnostics.dropped` field on the callback retains the rejected NACEs + reasons for analyst awareness. | Avoids tagging every NACE unconditionally and showing irrelevant briefs on dashboards (a furniture publication should not appear on bakery firm dashboards just because it mentioned a kitchen-appliance trend). | Tag every NACE unconditionally and let analysts curate post-hoc — rejected: customer dashboards would be noisy from day one and analyst review work increases; centralised relevance classifier (one LLM call returning all 4 verdicts) — rejected: trades parallelism for one fewer call but ties relevance + insights output formats together, complicates output parsers. | user |
| 2026-04-28 | D-031 | **v0.3 Track C scope expanded to 4 NACEs: 10 (bakery), 31 (furniture), 46 (wholesale of metal), 49 (freight). Supersedes D-027.** The new MagnusWeb cohort Excel covers all four; the n8n workflow runs four parallel branches; the relevance gate determines which subset of NACEs ends up tagged on each brief. NACE 49 broadened from "silniční nákladní doprava" only (4941) to "Nákladní doprava" in general (4941 + 4920 + 4950) per the Excel's labelling. | Multi-NACE Model B is structurally indifferent to NACE count; running 4 in parallel is the same workflow shape as 2. Stops the demo from looking artificially scoped. | Stay at single-NACE Track C — rejected: Model B's value (one brief reaches multiple cohorts) is invisible in single-NACE; expand to 6+ NACEs — rejected: no source data and no PoC value over 4. | user |
| 2026-04-29 | D-032 | **`pricing_power` → `roe` in the frozen-8 metric set. Amends D-024.** Migration 0012 adds `cohort_companies.roe NUMERIC(8,4)` + partial index. Migration 0013 swaps the `metric_id` CHECK on `owner_metrics` and `cohort_aggregates` to drop `pricing_power` and add `roe`. Plausibility envelope -100..+200 with 1 decimal place. Floor uses `GLOBAL_FLOOR=30` (not the strict-50 used for `working_capital_cycle`). Czech display: `12,4 %` with U+2212 minus for negatives. Derivation at ingest: `(HV za účetní období / Vlastní kapitál) × 100`. | The MagnusWeb Excel pre-computes ROE per firm; pricing_power had no real cohort column at all (synth-quintile-only since v0.3 Track B), occupying a slot without contributing real data. ROE is a standard SME comparator with broader analyst familiarity and direct derivability from the Excel's HV-za-účetní-období + Vlastní-kapitál columns. The 8-tile cap from D-024 stays intact; only the slot's identity changes. | Keep both as a 9th tile — rejected: 8-tile cap is product-locked; keep `pricing_power` as synth-only — rejected: occupies a slot without real-data benefit, divergence-prone vs. owner-supplied values; pick a different 8th metric (asset turnover / equity ratio / ROA) — rejected: ROE has the best balance of derivability + analyst familiarity at this moment. | user |

## How to add an entry

1. Use the next sequential `D-NNN` ID.
2. Keep **Decision** to one sentence.
3. **Rationale** cites PRD sections, principles, or upstream decisions — not opinion.
4. **Rejected alternatives** names each and why — never leave blank; write "None considered — reason" if so.
5. Append only; never edit past rows. If a decision is reversed, add a new row referencing the old ID.
